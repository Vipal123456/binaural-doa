# 模型 Method 叙事：v7 LiteCue 的设计逻辑

> 每一块组件都要回答三个问题：**解决什么？为什么这个方案？别的方案为什么不行？**

---

## 一、总体设计理念（Motivation 段落）

做双耳 DOA 的模型，输入天然有两类信息：

- **单耳内容**（log-magnitude spectrogram）：携带声源频谱特征、房间混响结构
- **双耳线索**（ILD、IPD、相干性）：携带声源**方向**的物理信息

一个自然的想法是把所有东西堆进一个 CNN。但这里有矛盾：单耳频谱模式是高维的（257 个频带 × 时间帧），需要足够的 CNN 深度来提取语义特征；而双耳线索是低维的物理量——ILD 在频率上平滑变化，IPD 由头相关传输函数的相位差唯一确定。**用同一个深层 CNN 去处理低维物理量是浪费参数，用同一个浅层 CNN 去处理高维频谱是不够的。**

所以核心设计就一句话：**非对称——内容编码器深，线索编码器浅。**

这个想法的来源是神经科学。哺乳动物听觉系统中，脑干的外侧上橄榄核（LSO）和内侧上橄榄核（MSO）专门处理 ILD 和 ITD 线索，神经元数量远少于处理频谱特征的耳蜗核和听皮层。进化没有把两者堆进同一个脑区——我们的网络也不应该。

---

## 二、组件叙事

### 2.1 共享权重的内容编码器

> **为什么共享？**

设计选择：左右耳通过**同一个** CNN。
备选方案：左右耳各用独立的 CNN，或者一个两倍输入通道的 CNN。

共享权重的理由有两个：

**物理对称性作为归纳偏置。** 左右耳在物理上是对称的。一个来自 30° 方向的声音，到达左耳和右耳的频谱是不同的（头部遮蔽效应），但这种"不同"是由头相关传输函数（HRTF）的方向性决定的，而不是左耳和右耳有本质不同的接收机制。共享编码器强制网络学习"方向无关"的频谱特征——同一个卷积核必须在左右耳信号上都工作，这样学到的特征必然是频谱的通用模式（谐波结构、共振峰等），而不是某个特定方向特有的模式。

**参数量减半。** 独立编码器直接翻倍参数量，对于轻量化目标是不可接受的。

> **为什么深度可分离卷积（V2 Balanced）？**

标准卷积（V1 encoder）在频率轴和时间轴上同时做 3×3 卷积，参数量 = in_ch × out_ch × 9。深度可分离卷积将其分解为逐通道的空间卷积 + 1×1 通道混合，参数量约为原来的 1/3。在频谱处理中，频率轴和时间轴的相关结构不同（频率轴需要降采样，时间轴需要保留），分离这两个维度让每个卷积核专注做一件事：先在频率上压缩，再在通道间混合。

---

### 2.2 内容关系模块：均值、差、绝对值差

> **为什么是这三个量？**

内容编码器输出 F_L 和 F_R。如何将它们融合为一个内容表示？

只取均值 `(F_L+F_R)/2` 丢掉了所有双耳差异信息。只取差值 `F_L-F_R` 丢掉了单耳共享的频谱结构（而这对识别声源类型、排除混淆有帮助）。三个一起：均值捕获共享频谱、差捕获方向性差异、绝对值差捕获能量不对称性（无论左耳还是右耳更大）。三者拼接后用 Linear + LayerNorm + ReLU 压缩到 80 维——让网络自己学线性组合的方向。

这本质上是把"内容编码器空间里的 ILD"（差值）和"内容编码器空间里的单耳频谱"（均值）解耦后重新组合。注意这里的差值是在 CNN 特征空间而非原始频谱空间的——CNN 已经提取了语义特征，所以这个差值是"高层次的双耳差异"，比原始频谱做差更有信息量。

> **为什么不做 attention/交叉注意力？**

（如果是原始 V1 `BinauralDOANet` 里做了 cross-attention，但 v7 里把它砍掉了）

交叉注意力（L attend to R, R attend to L）在理论上可以通过学习左右耳特征的对齐关系来捕获空间信息。但代价很大：多头注意力参数量 O(d²)，在小数据量下容易学到虚假相关。我们和 V1 的对比表明，简单的拼接+线性投影在 50h 数据上优于交叉注意力。这也符合"不要在小数据上用大算子"的一般原则。

---

### 2.3 线索编码器：为什么可以这么轻

> **核心论证：双耳线索在频率上是冗余的。**

ILD 和 IPD 作为频率的函数，其变化由头部衍射的物理过程决定。衍射是一个平滑的物理效应——ILD(f) 和 IPD(f) 在频率上没有突变。这意味着你不需要 257 个频带的完整分辨率：16 个频带的池化表示已经保留了几乎全部空间信息。

**证据**：人类听觉系统的频率分辨率在低频（<500Hz）非常好、高频较差，但空间定位主要依赖低频 ITD 和高频 ILD 的包络——而不是每个频率 bin 的精确值。听觉外周本身就在做频率池化。

所以 `LiteCueEncoder` 的设计——频率池化到 16 个频带 → 两层 1D 时序卷积（kernel=3）→ 线性投影——是有感知依据的。时序卷积的作用不是提取频率模式（线索在频率上已经池化了），而是**平滑时序上的噪声**。单帧 ILD/IPD 在噪声下抖动很大，但局部时间窗口内的移动平均是稳定的。

> **为什么只有 30K 参数？**

因为线索编码器不需要"理解"声源——那是内容编码器的任务。它只需要忠实地把物理量（ILD, IPD, 相干性）映射到一个向量空间。这是一个近乎线性的映射，两层卷积足够。

---

### 2.4 晚期融合（Concat）而非早期融合

> **为什么不在输入端直接堆叠线索和频谱？**

早期融合（输入端把 logmag、ILD、sin(IPD)、cos(IPD)、coherence 堆叠成多通道，送进一个统一的 CNN）是直觉上最简单的方案。我们的 `earlyfusion` 消融变体（158K）就是为此设计的。

但早期融合有一个根本问题：它迫使 CNN 同时学会"提取频谱模式"和"利用空间线索"——这两个任务对感受野和深度的要求完全不同。频谱模式需要大的频率感受野（跨越多个频带），空间线索需要保留频率轴的局部差异（ILD 在不同频带是不同的）。一个 CNN 很难同时做好这两件事。

晚期融合让内容编码器专注于频谱特征、线索编码器专注于空间信息，各自优化各自的目标，最后在拼接层让 GRU 去学如何整合。实验结果（earlyfusion MAE? vs litecue 14.57°）支持这个选择。

> **为什么不用门控或注意力融合？**

这也是你踩过的坑。dualcue 分支里的相干性门控本质上是一种"可靠性加权"——用相干性来调节 ILD/IPD 的贡献。理论上合理（相干性低 → 线索不可靠 → 降低权重），但实际没有增益。为什么？

推测是因为：在混响+噪声场景下，相干性本身也被污染了。高混响环境下，即使声源定位信息明确，相干性也可能因为后期反射而降低。用被污染的可靠性指标去门控被污染的定位线索，相当于"用一个错误去纠正另一个错误"。BiGRU 的隐状态反而能更有效地隐式学习每帧的可靠性——它看到的是整个时间上下文，比单帧相干性更可靠。

---

### 2.5 BiGRU + 注意力池化

> **BiGRU 的双向性为什么重要？**

DOA 不是一个逐帧独立的分类任务。声音在时间上是连续的——前一帧的方位角和当前帧的方位角强相关。单向 GRU 只能利用过去的信息，但未来的信息对定位同样重要。例如：

- 一个声音刚开始时，信号能量低，定位不确定；但知道"之后会有更响的部分"有助于回溯性判断
- 混响在时间上呈指数衰减，看到衰减模式有助于区分直达声和反射声

双向 GRU 让每个时间步都能看到完整的上下文。

> **注意力池化 vs 均值池化**

均值池化对所有时间帧一视同仁。但显然不是所有帧都同等重要——低 SNR 帧、混响拖尾帧的信息量远低于高 SNR 直达声帧。注意力池化学到一个 softmax 权重分布，自动关注信息量高的帧。这本质上是一种软的特征选择，不需要显式的 VAD 或 SNR 估计。

> **参数配置：hidden=128, 2 layers**

2 层而非 1 层增加了非线性深度。第一层 GRU 学局部时序模式（几十毫秒级），第二层学更全局的模式（秒级）。对于 2s 的片段，这个深度是合适的。更深（3-4 层）会增加参数且容易过拟合——我们试过（gru_num_layers 未显式消融，但 SDEL 的 128 维 GRU 是常见配置）。

---

### 2.6 前后辅助分类头

> **为什么需要显式的前后分类？**

72 类 azimuth 分类器对前后方向的区分是隐式的——前后共享同一个 bin 的 logits。但前后方向在很多情况下是定位失败的主要原因：10° 和 170° 的 ILD 完全相反（左耳大 vs 右耳大），但频谱模式可能因为房间混响而相似。加上一个显式的 2 分类（front vs back）辅助任务，让网络在中间层就不得不学习前后判别特征。损失函数里 `front_back_aux_weight=0.3`，这个权重不大，只起到正则化作用而非主导训练。

> **如果做 frontal-only（[-90°, 90°]），这个头就没用了**

这也是前面讨论的 frontal-only 数据集的一个重要简化。

---

## 三、简化线：从 dualcue 到 litecue

这是论文里最有叙事价值的部分。

**起点**：我们设计了一个看起来很合理的双分支线索编码器（dualcue）——定位线索（ILD, IPD）用一个编码器，可靠性线索（相干性）用另一个编码器，两者在特征层面融合。理论动机是神经科学中的"线索可靠性加权"假说。

**结果**：litecue（所有线索统一进一个编码器）在所有指标上与 dualcue 持平甚至略好（静态 14.57° vs 15.77°，运动持平）。相干性的分离编码没有产生可测量的增益。

**解释**：可能的原因有三。（1）混响场景下相干性本身被污染，不能可靠地指示线索质量；（2）BiGRU 的时序上下文隐式地承担了"可靠性估计"的功能——网络可以通过前后帧的一致性判断当前帧是否可信；（3）32 维的拼接特征空间足够表达线索与可靠性的交互，不需要显式的结构约束。

**叙事价值**："我们过度设计了，然后做了减法"——这比"我们设计了最优架构"更可信，也更符合科学研究的实际过程。

---

## 四、各组件的一句话价值总结

| 组件 | 一句话价值 |
|------|-----------|
| 共享内容编码器 | 物理对称性 → 方向无关特征 → 参数减半 |
| 深度可分离卷积 | 频率和时间解耦处理 → 1/3 参数量 |
| 均值+差+绝对值差 | 完整描述 L/R 关系，不多不少 |
| 轻量线索编码器（16 band + 1D conv） | 线索在频率上平滑冗余，不需要深层 CNN |
| 晚期拼接融合 | 让内容编码和线索编码各自专注，不做互相干扰的事 |
| BiGRU + 注意力池化 | 双向上下文 + 软性帧选择，无需显式 VAD |
| 前后辅助头 | 少量正则化信号，帮助解决前后歧义 |
| 单分支线索（litecue）胜过双分支（dualcue） | 不要用被污染的可靠性去门控被污染的线索 |
