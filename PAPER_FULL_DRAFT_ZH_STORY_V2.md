# 面向双耳 360° DOA 分类的结构化线索分解与紧凑融合

> 稿件状态：ICASSP 中文故事增强稿。本文数值仍对应 7,776 样本的原静态测试协议；speech-disjoint official split 的主模型结果虽已产生，但基线与消融尚未全部闭环，因此本稿不混用新旧协议。正式方法名、作者信息和部分参考文献仍待补充。

## 摘要

双耳 360° 声源定位需要同时利用耳间差异与方向相关频谱结构，但这些信息具有不同的物理含义和统计形式：ILD/IPD 直接描述左右耳差异，局部耳间相干性描述两耳观测的一致程度，而双耳幅度谱还保留源频谱和 HRTF 频谱着色。紧凑模型若在高维输入端统一处理这些异质信息，有限的表示容量将同时承担差异提取、上下文建模和线索整合。本文据此研究静态单说话人的 72 类水平 DOA 分类，并提出一种“先保持结构、后低维融合”的紧凑网络。共享轻量编码器首先提取两耳幅度谱表示，再以交换不变的均值和绝对差值、以及交换变号的带符号差值显式组织双耳关系；两个低维分支分别编码 ILD/IPD 方向线索值与局部相干性。三类表示随后由单层 BiGRU 和注意力池化聚合。在包含 7,776 个样本的当前静态 KEMAR 测试协议上，该模型以 0.153 M 参数和 0.391 GFLOPs 获得 96.51% 准确率与 2.46° 环形 MAE。消融结果显示，幅度谱上下文在显式线索之外仍提供互补信息，独立相干性编码与分离式线索编码也优于对应的移除或合并变体。结果支持的结论是：按线索结构分配有限建模能力，可以在当前协议下形成有竞争力的性能—效率权衡。

**关键词：** 双耳声源定位；到达方向估计；双耳空间线索；耳间相干性；轻量神经网络

## 1. 引言

双耳声源定位旨在从左右耳波形恢复声源方向，是空间音频、听觉辅助、机器人听觉与沉浸式通信中的基础能力 [待核验引用]。本文聚焦静态单说话人的完整水平面定位：输入为 2 s 双通道语音，输出为覆盖 360° 方位空间的 72 类片段级预测，相邻类别间隔 5°。这一任务并非普通的 72 类识别，因为类别首尾在几何上相邻，且左右耳提供的观测受到头部遮挡、耳廓滤波、房间反射和背景噪声共同影响。因此，模型既要正确命中离散方向，也要控制圆周上的大角度错误，并避免用过高计算量换取有限增益。

双耳定位的困难来自多个互补线索在退化条件下并不同步失效。ILD 和 IPD 分别刻画两耳幅度比与相位关系，是直接的方向证据；但反射声会把直达声与延迟副本叠加，加性噪声也会以不同强度污染各时频区域，使这些差异偏离理想自由场观测 [待核验引用]。更重要的是，若仅观察两耳之间的差异，前后方向可能产生相近的耳间关系，即经典的前后混淆或锥形混淆现象；区分它们还需要利用耳廓与头部引入的方向相关频谱着色 [待核验引用]。因此，显式耳间差异、局部两耳一致性和幅度谱上下文承担的是相关但并不相同的角色。

现有数据驱动方法通常从双耳波形、声谱图、GCC-PHAT、ILD/IPD 或相对传递函数中学习 DOA 映射 [待核验引用]。高容量时频网络能够让模型自行学习上述关系，但计算会随频率分辨率、序列长度和隐藏维度迅速增长；另一种直接做法是把所有特征沿通道维拼接后交给同一编码器，却没有显式保留它们的结构差异。这里不能未经对照就断言“早期融合必然失败”，真正可检验的问题是：在紧凑预算下，是否应让单耳频谱、方向线索值和相干性共享同一套表示容量，还是先用与其结构匹配的低成本路径编码，再进行融合？

本文的核心思想不是增加更多模块，而是把有限容量分配给三种不同结构。首先，两耳幅度谱经过共享编码器映射到可比较空间，再以均值、带符号差值和绝对差值构造关系表示；在交换左右耳时，均值和绝对差值保持不变，而带符号差值改变符号，从而显式保留共同成分、差异幅度与左右方向性。其次，ILD 与 IPD 经过低维方向线索分支编码，其中以 sin/cos 表示 IPD，避免相位在 $-\pi$ 与 $\pi$ 处的数值跳变。最后，局部耳间相干性由独立小分支编码，以保留归一化互谱关系中不同于线索取值本身的信息。三个分支不执行显式门控或置信度校准，而是在 112 维特征层融合后由紧凑时序头聚合。

现有实验证据支持三点贡献。第一，本文给出一种 0.153 M 参数、0.391 GFLOPs 的结构化紧凑网络，在当前 KEMAR 静态协议上获得 96.51% 准确率与 2.46° 环形 MAE；它不具有最高绝对精度，但位于较有利的性能—效率工作点。第二，仅保留显式线索时模型仍达到 95.95% 准确率，这一结果说明 ILD/IPD 已承载主要方向信息；完整模型进一步把 MAE 从 2.94° 降至 2.46°，支持幅度谱上下文提供互补证据，而非重复替代显式线索。第三，移除相干性分支或合并两类线索编码都会增加 MAE，支持“保持线索结构后再融合”这一设计判断。上述结论仅适用于当前模拟 KEMAR 静态协议，且尚无配对显著性检验，不能外推到未见 HRTF、真实录音、移动声源或硬件实时部署。

## 2. 相关工作

传统双耳定位可理解为从“差异线索”和“频谱线索”两类证据推断方向。双耳时间差、IPD 与 ILD 描述声波到达两耳的时间、相位和能量差异，GCC-PHAT 等方法则通过跨通道相关峰估计传播时延 [待核验引用]。这些线索对左右方位具有直接解释，但单凭耳间差异并不总能消除前后混淆；耳廓和头部产生的方向相关频谱着色因而构成补充信息 [待核验引用]。混响和扩散噪声进一步改变局部互谱结构，使理想直达声假设不再成立。

耳间相干性是归一化互谱幅度的一类局部统计量。与 IPD 给出的相位取值不同，相干性描述在局部时频邻域内两耳复谱关系的一致程度；它可能随直达声、反射声和非相干噪声的相对比例而变化 [待核验引用]。然而，相干性数值并不天然等于经过校准的“可靠概率”。本文因此只把它作为独立输入表示，不声称其显式抑制受污染的 ILD/IPD，也不把独立分支写成可靠性门控。

数据驱动双耳定位方法覆盖两种容量分配路线。一类在原始波形或完整时频网格上使用卷积与循环网络统一学习空间映射，减少手工假设，但高分辨率建模通常带来更高计算量；另一类先构造 GCC-PHAT、ILD/IPD 或 DP-RTF 等空间表示，再学习其到方向的映射，从而突出物理线索 [待核验引用]。本文评测的 SDEL-style、FN-SSL-style、DP-RTF-style 和 BiL-style 分别代表这些结构路线的项目内适配实现，而非原论文官方结果，因为复现忠实度和等预算调参尚未完成审计。本文位于两者之间：保留可解释的显式线索，同时用共享幅度谱编码补充方向相关上下文，并把高维建模限制在轻量、结构化路径中。其研究目标是验证这种容量分配能否形成更好的性能—效率权衡，而不是宣称绝对定位精度超过所有基线。

## 3. 所提方法

### 3.1 任务定义与声学特征

设左右耳 2 s 波形为 $x_L,x_R\in\mathbb{R}^{N}$，采样率为 16 kHz。记其复数短时傅里叶变换为 $X_L(t,f)$ 和 $X_R(t,f)$。本文采用 512 点 DFT、400 点 Hann 窗和 160 点帧移，得到 $T=201$ 个时间帧与 $F=257$ 个频点。类别标签 $y\in\{0,\ldots,71\}$ 对应固定方向

$$
\theta_y=-180^\circ+5^\circ y.
$$

模型输出片段级 logits $\mathbf{o}\in\mathbb{R}^{72}$，不执行连续角度回归。左右耳幅度谱上下文输入定义为

$$
M_e(t,f)=\log\bigl(|X_e(t,f)|+\epsilon\bigr),\qquad e\in\{L,R\}.
$$

方向相关的显式双耳特征为

$$
D(t,f)=20\log_{10}\frac{|X_L(t,f)|+\epsilon}{|X_R(t,f)|+\epsilon},\qquad
\Phi(t,f)=\angle\{X_L(t,f)X_R^*(t,f)\},
$$

其中 $D$ 为 ILD，$\Phi$ 为 IPD。直接使用 $\Phi$ 会使物理上相邻的 $-\pi$ 与 $\pi$ 在数值轴上相距 $2\pi$；因此方向线索值张量写为 $\mathbf{Q}_v=[D,\sin\Phi,\cos\Phi]$，用单位圆上的连续坐标表示相位。局部耳间相干性定义为

$$
C(t,f)=\frac{|\mathcal{S}\{X_LX_R^*\}|}{\sqrt{\mathcal{S}\{|X_L|^2\}\mathcal{S}\{|X_R|^2\}}+\epsilon},
$$

其中 $\mathcal{S}$ 表示采用反射填充的 $5\times3$ 频率—时间局部平均，最终将 $C$ 截断到 $[0,1]$。该归一化把局部互谱幅度与两耳局部功率联系起来，使 $C$ 与单纯的相位取值或幅度比不同；本文将这种差异作为分支设计的动机，而不把 $C$ 解释为校准概率。

### 3.2 结构概览

如模型图所示，所提方法包含幅度谱上下文流、方向线索值分支、相干性分支和时序融合头。其设计遵循三个原则。第一，左右耳幅度谱具有相同的数据类型和频率结构，适合由共享编码器提取可比较表示；但两耳之间的关系需要显式组织，否则后续网络仍需从拼接特征中重新学习交换对称性。第二，ILD/IPD 已经是逐时频点的耳间差异，不需要重复使用高容量二维主干重新发现两耳关系，因而先压缩频带再进行轻量时序编码。第三，相干性与线索取值含义不同，先独立编码可避免在输入端抹平这种差异，随后再交由统一时序头学习组合。该分解是一种可检验的结构先验，而不是由模块名称自动成立的机制结论；第 4.6 节分别通过移除幅度谱上下文、移除相干性及合并线索编码进行验证。

### 3.3 幅度谱上下文流

幅度谱上下文流用于保留显式 ILD/IPD 没有完整表达的信息。单耳对数幅度谱既包含说话人和语音内容造成的谱包络，也包含 HRTF、房间和噪声造成的方向相关或环境相关着色；因此它不能被简单称为“纯内容特征”。本文使用同一个轻量卷积编码器 $E_c$ 处理两耳对数幅度谱：

$$
\mathbf{F}_L=E_c(M_L),\qquad \mathbf{F}_R=E_c(M_R),\qquad
\mathbf{F}_e\in\mathbb{R}^{T\times64}.
$$

$E_c$ 包含三个通道数依次为 16、24 和 32 的轻量卷积阶段。每个阶段依次使用 $1\times1$ 逐点卷积、$3\times3$ 深度卷积、步长为 $(1,2)$ 的 $1\times3$ 深度卷积及 $1\times1$ 逐点卷积，并配合批归一化、ReLU 和 dropout。降采样仅发生在频率轴，因而保留 2 s 片段的帧级时间分辨率；随后通过自适应频率平均池化与线性投影得到 64 维逐帧表示。左右耳共享权重一方面避免为同类输入维护两套参数，另一方面使 $\mathbf{F}_L$ 与 $\mathbf{F}_R$ 位于同一特征坐标系，从而可以直接构造逐维关系。这里的“可比较性”来自共享映射这一结构事实；由于没有非共享编码器对照，本文不声称共享权重本身已被证明提高定位指标。

两耳编码结果通过三种确定性关系组合：

$$
\mathbf{R}_c=\left[\frac{\mathbf{F}_L+\mathbf{F}_R}{2},\ \mathbf{F}_L-\mathbf{F}_R,\ |\mathbf{F}_L-\mathbf{F}_R|\right].
$$

这三个关系具有明确的耳交换行为。交换左右耳时，均值保持不变，带符号差值改变符号，绝对差值仍保持不变；因此该表示同时暴露与耳顺序无关的共同成分和差异强度，以及随左右方向反转的有符号关系。三者拼接后经线性层、层归一化、ReLU 与 dropout 投影为 $\mathbf{H}_c\in\mathbb{R}^{T\times80}$。这种构造减少了后续网络从任意拼接中自行学习基础代数关系的负担，但当前实验只验证了整个幅度谱上下文流的增益，尚未分别证明三个关系项各自不可替代，也没有验证严格的耳交换等变性。

### 3.4 方向线索值与相干性分支

方向线索值分支负责压缩并编码 $\mathbf{Q}_v$。ILD 与 IPD 已由左右耳复谱直接计算，网络的任务不再是从两路输入中发现差异，而是整合这些差异随频率和时间的分布。该分支先把 257 个频点自适应平均池化为 16 个频带，再使用两层核大小为 3 的一维时间卷积，得到 $\mathbf{H}_v\in\mathbb{R}^{T\times24}$。每层卷积后采用批归一化和 ReLU，两层之间使用 dropout。这种“先频率压缩、后时间编码”的顺序将高成本时序建模限制在低维频带表示上；当前复杂度结果支持其低成本实现，但没有参数匹配实验单独证明 16 带划分优于其他频率分辨率或能直接提高抗噪性。

相干性分支以 $C$ 为输入，采用相同的 16 频带压缩与两层时序卷积结构，输出 $\mathbf{H}_r\in\mathbb{R}^{T\times8}$。设置独立分支并不是因为相干性被预先定义为“可信/不可信”标签，而是因为归一化互谱幅度与 ILD/IPD 的取值空间不同；较小的输出维度则限制了该辅助信息占用的容量。需要强调的是，当前实现不利用 $\mathbf{H}_r$ 生成门控权重，也不对 $\mathbf{H}_v$ 进行显式重标定；两者仅通过

$$
\mathbf{H}_q=[\mathbf{H}_v,\mathbf{H}_r]\in\mathbb{R}^{T\times32}
$$

直接拼接。因此，消融实验能够支持“独立相干性编码在当前模型中有益”和“其与方向线索分开编码优于所测合并实现”，但不能支持“模型已经学得校准置信度”或“相干性显式抑制了受污染线索”等更强机制主张。

### 3.5 时序融合与训练目标

幅度谱上下文表示和双耳线索表示在特征维拼接，并经过层归一化和 dropout：

$$
\mathbf{Z}=\operatorname{Dropout}\left(\operatorname{LN}\left([\mathbf{H}_c,\mathbf{H}_q]\right)\right)
\in\mathbb{R}^{T\times112}.
$$

融合被推迟到各分支已经压缩为帧级表示之后，因此单层 BiGRU 只需处理 112 维输入，而不是完整的多通道时频平面。BiGRU 在每个方向使用 80 个隐藏单元生成序列 $\{\mathbf{u}_t\}$；双向建模适用于当前离线、整段可见的 2 s 分类设定，但不意味着模型满足因果实时要求。注意力池化通过两层打分网络 $g(\cdot)$ 计算

$$
a_t=\frac{\exp(g(\mathbf{u}_t))}{\sum_j\exp(g(\mathbf{u}_j))},\qquad
\bar{\mathbf{u}}=\sum_t a_t\mathbf{u}_t,
$$

再由线性分类器输出 72 类 DOA logits。训练阶段还使用二分类前后辅助头，总损失为

$$
\mathcal{L}=\operatorname{CE}_{\mathrm{LS}}(\mathbf{o},y)
+0.3\operatorname{CE}(\mathbf{o}_{\mathrm{fb}},y_{\mathrm{fb}}),
$$

其中 $\operatorname{CE}_{\mathrm{LS}}$ 的标签平滑系数为 0.1。主任务推理仍以 72 类输出为准。由于尚无三随机种子的辅助头开关对照，本文不把前后辅助头单独列为已验证贡献。

## 4. 实验

### 4.1 数据集与信号生成

本文使用 LibriSpeech train-clean-100 语音、KEMAR HRTF 和 SofaMyRoom 房间响应生成静态双耳样本，并使用 DEMAND 场景噪声构造扩散场双通道背景。每条样本时长为 2 s，包含一个说话人和一个固定水平面方位。训练集包含 36,000 条样本（20 h），验证集包含 7,200 条样本（4 h），测试集包含 7,776 条样本（4.32 h）；三个集合在 72 个类别上均衡。

训练与验证的噪声场景为 TMETRO、PCAFETER 和 OOFFICE，SNR 从 $[-10,10]$ dB 均匀采样。测试集使用未在训练和验证中出现的 TBUS、NPARK 和 SPSQUARE 场景，并包含 clean、10、5、0、$-5$ 和 $-10$ dB 六种条件。测试协议覆盖六个固定房间，目标 RT60 为 0.30--0.80 s，声源距离约为 1--2 m。所有集合均使用 KEMAR，因此该实验不评估未见 HRTF 泛化。

当前结果还存在语音划分限制：测试集 6,784 个唯一语音路径中有 4,868 个也出现在训练集，占约 71.8%。这不等同于已证实发生标签泄漏，因为方位标签由独立渲染产生，但它阻止本文声称说话人或语句无关泛化。因此，表 1--3 仅代表当前原协议；speech-disjoint 协议需要重新训练和评估全部模型，不能通过重新统计旧预测得到。

### 4.2 实验设置

所提模型使用 AdamW 优化器，初始学习率为 $5\times10^{-4}$，权重衰减为 $10^{-4}$；余弦调度最低学习率为 $10^{-5}$，最多训练 100 个 epoch，早停耐心值为 15。batch size 为 64，梯度裁剪阈值为 1.0，dropout 为 0.2。主结果与消融均报告三个随机种子 42、43 和 44 的均值与总体标准差。

比较对象包括 SDEL-style、FN-SSL-style、DP-RTF-style 和 BiL-style。所有名称均指项目内复现，尚未完成原论文结构、输入特征和调参预算的忠实度审计。FN-SSL-style 的三次结果还混合了辅助头开启和关闭设置，因此与其差异只能作为当前复现下的对比，不能解释为对原方法的确定性优势。训练硬件、训练时间、软件版本、实际停止 epoch 和 checkpoint 哈希仍待补充。

### 4.3 评价指标

Accuracy 表示 72 类完全命中率。对于预测类别 $\hat y$，先按 $\hat\theta=-180^\circ+5^\circ\hat y$ 映射到固定离散方向，再计算环形角距离

$$
d_{\mathrm{circ}}(\hat\theta,\theta)=
\left|\left((\hat\theta-\theta+180^\circ)\bmod360^\circ\right)-180^\circ\right|.
$$

MAE 为该距离在测试集上的平均值，取值范围为 $[0^\circ,180^\circ]$。因此本文 MAE 衡量离散类别预测的圆周角误差，不代表连续回归精度。复杂度报告参数量、乘加运算数（MACs）和浮点运算数（FLOPs）。

### 4.4 不同 SNR 下的定位性能

表 1 比较五个含噪 SNR 条件，目的在于观察各方法随噪声增强的退化趋势；clean 条件不列入该表。所提方法在 10 dB 至 $-10$ dB 间的 Accuracy 从 98.89% 降至 90.15%，MAE 从 0.87° 增至 6.30°。SDEL-style 在整体指标上更强，因此本文不能宣称绝对定位性能最佳。所提方法在所有 SNR 下均优于当前 DP-RTF-style 和 BiL-style 复现的两项指标；相对 FN-SSL-style，则不存在覆盖全部条件的一致优势。这些结果表明模型在噪声增强时保持渐进退化，但不证明相干性分支在低 SNR 下具有更强作用，因为尚无按 SNR 分解的消融结果。

**表 1　不同 SNR 下的静态定位结果。每项为三次运行平均 Accuracy（%）/ 环形 MAE（°）。**

| 方法 | 10 dB | 5 dB | 0 dB | -5 dB | -10 dB |
|---|---:|---:|---:|---:|---:|
| 所提方法 | 98.89 / 0.87 | 98.56 / 1.37 | 97.33 / 2.12 | 94.42 / 3.63 | 90.15 / 6.30 |
| SDEL-style | 99.07 / 1.20 | 99.05 / 0.92 | 97.99 / 1.74 | 95.47 / 3.44 | 91.77 / 5.35 |
| FN-SSL-style | 98.71 / 1.22 | 98.61 / 1.29 | 97.38 / 2.11 | 95.45 / 2.88 | 91.20 / 6.04 |
| DP-RTF-style | 98.05 / 2.59 | 97.25 / 3.07 | 95.99 / 4.33 | 94.39 / 5.21 | 89.09 / 7.51 |
| BiL-style | 97.81 / 2.33 | 96.63 / 3.59 | 94.80 / 4.66 | 91.05 / 7.51 | 84.67 / 12.74 |

### 4.5 性能与计算效率权衡

表 2 比较完整测试集定位结果与模型复杂度，目的在于判断所提结构是否以较小预算保留了有竞争力的性能。所提方法是当前比较中参数量、MACs 和 FLOPs 最低的模型。相较 SDEL-style，其参数量减少约 83.5%，FLOPs 减少约 72.1%，代价是 Accuracy 低 0.67 个百分点、MAE 高 0.28°。相较 FN-SSL-style，其 FLOPs 减少约 99.4%，即当前统计下约为后者的 $1/168$，同时 Accuracy 低 0.24 个百分点、MAE 高 0.13°。因此实验支持“性能—效率权衡”，但不支持“已经满足实时部署要求”，因为尚未测量目标硬件延迟、峰值显存或能耗。

**表 2　模型复杂度与完整静态测试集结果。定位指标为三次运行均值。**

| 方法 | 参数量（M）↓ | MACs（G）↓ | FLOPs（G）↓ | Accuracy（%）↑ | MAE（°）↓ |
|---|---:|---:|---:|---:|---:|
| 所提方法 | **0.153** | **0.195** | **0.391** | 96.51 | 2.46 |
| SDEL-style | 0.926 | 0.701 | 1.402 | **97.18** | **2.18** |
| FN-SSL-style | 0.659 | 32.843 | 65.687 | 96.75 | 2.33 |
| DP-RTF-style | 0.877 | 4.107 | 8.215 | 95.49 | 4.22 |
| BiL-style | 0.865 | 1.559 | 3.117 | 93.97 | 5.41 |

### 4.6 消融实验

表 3 检验三类信息分解是否对当前完整结构有益。移除幅度谱上下文流后，Accuracy 下降 0.56 个百分点，MAE 增加 0.48°；这说明在 ILD/IPD 线索仍保留的情况下，幅度谱上下文提供了互补信息。移除相干性分支后，Accuracy 下降 0.55 个百分点，MAE 增加 0.49°，支持独立相干性编码在当前组合中有益。将方向线索值与相干性送入同一个合并编码器后，Accuracy 下降 0.35 个百分点，MAE 增加 0.59°，表明当前分离式编码优于所测试的合并实现。由于尚未进行配对置信区间或显著性检验，本文不把这些差异描述为统计显著，也不据此声称三个分支分别学得了预设的物理功能。

**表 3　完整静态测试集上的消融结果。数值为三次运行的均值 ± 总体标准差。**

| 变体 | Accuracy（%）↑ | MAE（°）↓ |
|---|---:|---:|
| 完整模型 | **96.51 ± 0.10** | **2.46 ± 0.06** |
| 移除幅度谱上下文流 | 95.95 ± 0.24 | 2.94 ± 0.16 |
| 移除相干性分支 | 95.96 ± 0.14 | 2.95 ± 0.03 |
| 合并线索编码器 | 96.16 ± 0.18 | 3.05 ± 0.19 |

这组三项消融共同形成了本文最重要的经验性结论。首先，无幅度谱上下文的线索模型仍达到 95.95% Accuracy，说明显式 ILD/IPD 已经承担了大部分方向判别，而不是依赖大型网络从原始谱图中重新发现全部空间关系。其次，加入幅度谱上下文和相干性后，完整模型进一步减少圆周角误差，表明“方向线索值”“幅度谱上下文”和“局部两耳一致性”不是完全冗余的三种输入。最后，合并线索编码器的参数路径并未带来更低误差，反而使 MAE 高于分离结构；在当前对照范围内，这支持先保持线索结构、再低维融合的容量分配原则。该推理链解释的是观察到的模型行为，不等同于证明每个分支内部一定恢复了某个预设声学变量。

## 5. 结论

本文研究静态单说话人 360° 水平双耳 DOA 分类，并提出一种紧凑的结构化信息分解方法。其核心不是增加更复杂的主干，而是按信息结构分配有限容量：共享编码器组织两耳幅度谱上下文，低维分支分别处理 ILD/IPD 方向线索值和耳间相干性，随后通过单层 BiGRU 与注意力池化完成片段级融合。该设计把高维时频输入压缩为 112 维帧级表示后再执行统一时序建模。

在当前模拟 KEMAR 静态协议上，所提方法以 0.153 M 参数和 0.391 GFLOPs 取得 96.51% Accuracy 与 2.46° 环形 MAE。复杂度对比支持其相对于较大复现模型的性能—效率权衡。更重要的是，消融结果呈现出一致的证据链：显式 ILD/IPD 已提供主要方向信息，幅度谱上下文仍可降低误差，相干性也带来额外增益，而合并两类线索编码会削弱所测性能。由于 SDEL-style 仍取得更好的绝对定位指标，本文不主张整体最优性能。

当前证据的主要边界包括模拟数据、固定 KEMAR、原协议中的语音路径重叠、尚未完成的基线忠实度审计、FN-SSL-style 配置不完全一致，以及缺少统计显著性检验。后续首先应在 speech-disjoint 划分上重新训练和评估全部模型，再补充未见 HRTF 或真实双耳录音测试。目标硬件延迟和内存测量完成前，也不应将低 FLOPs 直接等同于实时部署能力。

## 参考文献

当前不列出未经逐条核验的文献。正式投稿前需要完成文献存在性、作者、题名、年份、页码和 DOI 核验，并用真实引用替换全文的 `[待核验引用]`。

---

## 内部审查记录（不纳入投稿正文）

### 反向提纲

1. **任务与困难：** 静态单说话人 360° 双耳 DOA 分类受到噪声、混响和前后混淆影响。
2. **知识观察：** 耳间差异、幅度谱上下文与局部互谱一致性具有不同的交换行为、数值拓扑和声学含义。
3. **研究缺口：** 紧凑模型不宜让同一高维编码器无约束地承担差异提取、上下文建模和线索整合，而应检验结构化容量分配。
4. **方法响应：** 三类信息分别编码，随后通过 112 维融合表示和轻量时序头完成分类。
5. **实验验证：** 分 SNR 表刻画噪声退化，复杂度表验证效率工作点，消融表建立“显式线索主导、上下文与相干性互补、分离编码有益”的证据链。
6. **证据边界：** 不扩展到尚未闭环的新 speech-disjoint 全套结果、未见 HRTF、真实录音、移动源、显著性或硬件实时性。

### Claim-Evidence Map

| 主张 | 证据 | 状态 |
|---|---|---|
| 所提方法具有较低模型复杂度 | 0.153 M 参数、0.195 G MACs、0.391 G FLOPs，均低于参评复现 | 已支持 |
| 所提方法获得具有竞争力的静态定位性能 | 完整测试集 96.51% Accuracy、2.46° 环形 MAE；但低于 SDEL-style | 已支持，已限制措辞 |
| 幅度谱上下文流提供互补信息 | 移除后 Accuracy 降至 95.95%，MAE 升至 2.94° | 已支持于当前组合 |
| 三类输入具有不同数学结构 | IPD 的环形表示、相干性的归一化互谱定义、耳交换下关系项的确定性变化 | 实现与定义支持；属于设计依据，不是性能因果结论 |
| 独立相干性分支有益 | 移除后 Accuracy 降至 95.96%，MAE 升至 2.95° | 已支持于当前组合 |
| 分离式线索编码优于当前合并实现 | 合并编码器为 96.16% / 3.05°，完整模型为 96.51% / 2.46° | 已支持于所测变体 |
| 相干性分支在低 SNR 下作用更强 | 无按 SNR 的分支消融 | 缺少证据，不写入正文 |
| 模型可泛化到新说话人、未见 HRTF 或真实房间 | 当前语音路径重叠且全部使用 KEMAR 模拟数据 | 缺少证据，不写入正文 |
| 模型适合实时或嵌入式部署 | 只有 FLOPs/参数量，没有延迟、内存或能耗 | 缺少证据，不写入正文 |

### 五维自审

- **贡献：** 主线统一为“依据线索结构分配有限表示容量”，并以性能—效率结果和组合消融支撑，没有把普通组件本身包装成独立发明。
- **写作清晰度：** 各段首句承担任务定义、知识观察、研究缺口、方法或证据功能；术语统一为幅度谱上下文流、方向线索值分支、相干性分支和时序融合头。
- **实验强度：** 主结果、复杂度和三项消融均有证据，但没有显著性检验，FN-SSL-style 配置仍不完全一致。
- **评价完整性：** 已报告不同 SNR、完整测试集和复杂度；缺少 speech-disjoint 重跑、未见 HRTF、真实录音、硬件延迟和失败案例。
- **方法合理性：** 公式与当前代码路径一致；机制性解释被限定为设计动机，未把拼接误写成门控或校准置信度。

### 从另一版初稿保留与删除的内容

- **保留：** “先分解、后融合”的叙事；轻量内容编码器的具体结构；方向线索值与相干性分开编码的动机；168 倍 FLOPs 差异；明确的局限性段落。
- **删除或降级：** 未核验编号引用；未确认的 `LiteRCNet` 名称；“首次”“核心创新”“物理上必然可靠”等强表述；相干性显式门控或校准置信度；低 SNR 消融优势；嵌入式可部署性；“mean/diff/absdiff 可表达任意双耳关系”；未经实验支持的频带压缩抗噪机制。
