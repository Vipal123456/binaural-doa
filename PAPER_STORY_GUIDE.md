# 论文叙事指南：如何讲好模型故事

## 一、核心叙事弧线

好的 ML 论文不是罗列实验结果，而是讲一个 **"我们发现了什么问题 → 尝试了什么 → 什么有效/什么无效 → 最终方案是什么"** 的故事。你的项目天然有一条很好的故事线：

```
大模型（SDEL, 927K）效果好但太重
        ↓
我们想做轻量化，但简单砍参数会掉性能
        ↓
关键洞察：双耳线索（ILD/IPD）信息密度高，不需要大 CNN 来提取
        ↓
第一版尝试：双分支（定位+可靠性分离）→ 过度设计，没卵用（诚实报告）
        ↓
简化：单分支 litecue → 效果一样，结构更简单
        ↓
最终方案：200K 参数 ≈ SDEL 的 22%，性能达到 70-80%
```

**这个故事的张力在于**：你做了一些"聪明"的设计（dualcue 分支分离），然后诚实地承认它没用，最终简化到一个更优雅的方案。审稿人喜欢诚实报告负结果的论文。

---

## 二、论文各部分叙事建议

### 2.1 Introduction 的叙事逻辑

标准三段式：

**第一段：问题是什么**
> 双耳 DOA（声源定位）是机器人听觉、AR/VR 等应用的基础任务。现有方法（SDEL、BiL）效果好但参数大（~1M），部署到嵌入式设备困难。轻量化模型存在，但通常在噪声鲁棒性或前后判别上显著退化。

**第二段：为什么难 + 我们的洞察**
> 轻量化的难点在于：内容特征（频谱模式）需要足够的 CNN 深度来提取，但双耳线索（ILD、IPD）本质上是低维的物理量——耳间声级差和相位差在频率上平滑变化，不需要深层 CNN。**关键洞察：内容编码器和线索编码器应该不对称设计——内容深、线索浅。**

**第三段：我们做了什么 + 主要结果**
> 我们提出 v7 litecue，一个非对称双流网络。内容流使用深度可分离 CNN（~170K），线索流仅使用频率池化+两层 1D 卷积（~30K）。实验表明：（1）litecue 以 200K 参数达到 SDEL（927K）性能的 75%，参数效率提升 4.6×；（2）我们尝试了更复杂的分支设计（分离定位/可靠性线索），发现它没有任何收益——"简单就是最好的"；（3）在运动声源场景下，模型保持了与静态一致的相对排序。

### 2.2 Related Work 的定位

不要罗列所有相关论文，而是要**建立对比坐标系**：

```
                   参数多 ↑
                         SDEL ●
                         BiL  ●
                         
         基于GCC-PHAT ←──┼──→ 基于学习
                         │
                         │  ★ v7 litecue (我们)
                         │  FAViT ●
                   参数少 ↓
```

**叙述**：现有方法要么（a）参数量大效果好（SDEL），要么（b）参数量小但依赖手工特征泛化差（GCC-PHAT 在运动场景崩溃），要么（c）用 Transformer 但数据不够训练不充分（FAViT 37° MAE）。**我们填补了"轻量 + 学习 + 泛化"的空隙。**

### 2.3 Method 的叙事

不要直接贴架构图然后罗列组件。而是按照**设计决策的顺序**来讲：

> **Step 1: 我们先用了一个共享的内容编码器。** 为什么共享？因为左右耳对声源的空间响应是对称的，共享权重强制模型学习方向无关的频谱特征。
>
> **Step 2: 然后我们需要融合左右耳信息。** 简单的做法是相减（ILD 本质上就是 logmag 的差），但频谱差里还隐含了 IPD 无法直接获取的信息。所以我们用了 `cat(均值, 差, |差|)` 三重拼接，让网络自己学该用哪个。
>
> **Step 3: 接下来是线索流。** 我们一开始想得很复杂——ILD/IPD 是"定位线索"，相干性是"可靠性线索"，应该分开编码再融合（dualcue）。后来发现这纯粹是过度设计——把它们丢进同一个轻量编码器效果一样好（litecue）。**这里我们报告了一个负结果：基于相干性的可靠性门控在当前任务上不产生增益。**
>
> **Step 4: 融合。** 内容和线索直接拼接，不做花哨的 gate/attention。原因是内容特征和线索特征的信息维度互补，简单拼接让后续的 GRU 自己去学如何整合。
>
> **Step 5: 时序建模。** BiGRU + 注意力池化。我们没有用更复杂的序列头（试了 SDEL 式的双向乘法，参数翻 3 倍只换来 <1° 改善，已废弃）。

### 2.4 Experiments 的叙事

实验部分的叙事逻辑应该是**逐层递进**：

**Table 1: 主结果（静态 + 运动）**
- 一句话：我们 200K 参数 > SDEL 927K 的 75% 性能， > BiL 866K 的绝对性能
- 同时报告 v7_dualcue 和 v7_litecue 并列，说明简化版更好

**Table 2: 消融实验**
- content_only vs litecue：量化线索的贡献（待补数据）
- dualcue vs litecue：证明分支分离无效
- earlyfusion vs litecue：证明晚期融合优于早期融合（如果有数据）

**Table 3: 噪声鲁棒性（SNR 扫描）**
- 一句话：v7 在低 SNR 下与 SDEL 差距拉大但不是崩溃性的
- 指出非稳态噪声（公园、食堂）是主要弱点

**Table 4: 不同噪声场景**
- OOFFICE/TBUS（稳态）→ v7 接近 SDEL
- NPARK/SPSQUARE（非稳态）→ 差距达到 6-8°
- 这是 limitation，也是 future work 的方向

### 2.5 Discussion / Limitation

诚实，但不要自我贬低：

> **局限性讨论（正面框架）：**
>
> 1. **噪声鲁棒性差距。** v7 在非稳态噪声场景（如公园、广场）下与 SDEL 的 MAE 差距达 8°。分析认为这是因为轻量线索编码器仅用 16 个频带做频率池化，对宽带非稳态噪声的频率选择性不足。未来可考虑自适应频带选择或多分辨率编码。
>
> 2. **前后混淆。** 所有模型（包括 SDEL）在后方区域的 MAE 均显著高于前方。部分原因可能是 CIPIC 数据集在后方方向的空间采样较稀疏。使用全空间均匀采样的 HRTF 数据库（如 BiLi）可能缓解此问题。
>
> 3. **静态 → 运动泛化。** 虽保持了相对排序，但绝对 MAE 从静态 14.6° 退化到运动 9.1°（注意：运动任务有 40 帧/样本的结构优势，不能直接比较）。这是因为运动场景下每帧的有效信息更少（100ms vs 2s）。

---

## 三、论文的核心贡献（Contribution）

应该精确、不过度宣称：

1. **轻量非对称双流架构**：200K 参数，参数效率是 SDEL 的 4.6×
2. **系统性消融**：证明复杂的分支设计（定位/可靠性分离）不必要，简单的单分支线索编码器足够
3. **全面的噪声鲁棒性分析**：SNR 扫描 + 6 种噪声场景的逐场景分解，揭示了轻量模型的主要弱点
4. **静态+运动双场景验证**：同一架构在两种场景下保持了与 baselines 一致的相对排序

---

## 四、卖点提炼

审稿人通常只看 Abstract + Figure 1 + Table 1 就形成判断。这三样东西必须讲清楚同一件事：

**Abstract one-liner:**
> 我们提出了一种 200K 参数的双耳 DOA 网络，通过非对称的内容-线索双流设计，以 SDEL 22% 的参数量达到其 75% 的性能。系统性消融表明，简单的单分支线索编码优于更复杂的分支设计。

**Figure 1（建议）：**
- 左边：模型架构图（内容流 + 线索流 → 拼接 → GRU）
- 右边：参数 vs MAE 散点图，v7 在左下角（低参数、低 MAE），SDEL 在中间偏右

**Table 1（主结果表）：**
- 静态：SDEL 11.3° → v7 14.6°（差距 3.3°），但参数 927K → 203K（4.6× 效率）
- 运动：SDEL 7.3° → v7 9.1°（差距 1.8°），但参数 927K → 189K
- 关键：不比绝对性能，比**参数效率**

---

## 五、当前缺口

在开始写论文之前，建议补上以下数据：

| 优先级 | 内容 | 目的 |
|--------|------|------|
| **P0** | content_only 测试结果 | 消融实验核心——量化线索的贡献 |
| **P0** | earlyfusion 测试结果（如果有 checkpoint） | 证明晚期融合 > 早期融合 |
| P1 | v7_dualcue 的逐 SNR 结果 | 可以和 SDEL 对比噪声鲁棒性 |
| P1 | 运动场景的逐 SNR 结果 | 展示运动+噪声双重挑战下的表现 |
| P2 | per-speed-bin 的 breakdown | 运动场景的细粒度分析 |
| P2 | Per-region confusion matrix 分析 | 前后混淆的具体模式 |

要我帮你补跑这些实验吗？
